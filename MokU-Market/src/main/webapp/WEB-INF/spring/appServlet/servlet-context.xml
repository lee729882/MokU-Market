<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- ✅ Spring MVC @Controller, @RequestMapping 등 활성화 -->
    <annotation-driven />

    <!-- ✅ 정적 리소스 매핑 (CSS, JS, images 등) -->
    <resources mapping="/resources/**" location="/resources/" />

    <!-- ✅ View Resolver: JSP 위치 지정 -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>

    <!-- ✅ Controller 패키지 자동 스캔 -->
    <context:component-scan base-package="com.MokU.controller" />

    <!-- ✅ 파일 업로드 설정 (multipart/form-data) -->
    <beans:bean id="multipartResolver"
        class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 최대 업로드 용량 (50MB = 52428800 byte) -->
        <beans:property name="maxUploadSize" value="52428800" />
        <!-- 기본 인코딩 -->
        <beans:property name="defaultEncoding" value="UTF-8" />
    </beans:bean>
<resources mapping="/upload/**" location="/upload/" />

</beans:beans>
