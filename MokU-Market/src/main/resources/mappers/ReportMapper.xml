<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.MokU.mapper.ReportMapper">

    <!-- 신고 등록 (상품 신고 기준) -->
<insert id="insertReport" parameterType="com.MokU.vo.ReportVO">
    INSERT INTO REPORT (
        REPORT_ID,
        REPORTER_ID,
        TARGET_USER_ID,
        PRODUCT_ID,
        POST_ID,
        CHAT_ROOM_ID,
        REASON_TYPE,
        DESCRIPTION,
        STATUS,
        CREATED_AT
    )
    VALUES (
        SEQ_REPORT.NEXTVAL,       <!-- ✅ 시퀀스 이름 DB와 동일 -->
        #{reporterId},
        #{targetUserId},
        #{productId,  jdbcType=NUMERIC},
        #{postId,     jdbcType=NUMERIC},
        #{chatRoomId, jdbcType=NUMERIC},
        #{reasonType},
        #{description},
        #{status},
        SYSTIMESTAMP
    )
</insert>




<!-- 신고 목록 조회 -->
<select id="selectReports" parameterType="string"
        resultType="com.MokU.vo.ReportVO">
    SELECT
        report_id,
        reporter_id,
        target_user_id,
        product_id,
        post_id,
        chat_room_id,
        reason_type,
        description,
        status,
        processed_by,
        processed_at,
        created_at
    FROM report
    <where>
        <if test="status != null and status != ''">
            status = #{status}
        </if>
    </where>
    ORDER BY created_at DESC
</select>

<!-- 신고 상태 변경 -->
<update id="updateReportStatus"
        parameterType="com.MokU.vo.ReportVO">
    UPDATE report
       SET status       = #{status},
           processed_by = #{processedBy},
           processed_at = SYSTIMESTAMP
     WHERE report_id    = #{reportId}
</update>
<!-- 같은 유저가 같은 상품을 이미 신고했는지 카운트 -->
<select id="countProductReportByUser" resultType="int">
    SELECT COUNT(*)
    FROM REPORT
    WHERE REPORTER_ID = #{reporterId}
      AND PRODUCT_ID  = #{productId}
</select>


</mapper>
